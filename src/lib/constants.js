// Shared scan timing. The scan screen's progress ticker and the visuals'
// settle animations both key off this so the needle/bars finish settling
// right as the scan completes.
export const SCAN_DURATION_MS = 6500
